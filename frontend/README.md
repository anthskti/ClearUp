npm run dev runs frontend

ProductListClient calls:

- getProductsByCategory: output the list via category
- getProductById: links the product to the product page

ProductClient calls:

- getProductId: to output its product information
- getMerchantsByProductId: gets products merchant list
- addMerchantByProductId: person can add a merchant through the client

BuilderClient calls:

- getRoutineById: displays routine information for page
- getRoutineWithProducts:
